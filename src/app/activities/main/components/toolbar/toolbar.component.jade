ts-toolbar
  .ts-toolbar__group
    .ts-toolbar__item
      .ts-toolbar__title
        ts-button.default(
          title="TrickySMM",
          ng-click="$ctrl.onMainActivityOpen()")

  .ts-toolbar__group
    .ts-toolbar__item
      ts-button.default(
        title="New Post",
        ng-click="$ctrl.onNewPostActivityOpen()")
    .ts-toolbar__item
      ts-button.round(
        svg="icon-accounts",
        ng-click="$ctrl.onProjectAccountsActivityOpen()")
